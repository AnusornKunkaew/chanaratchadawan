<!DOCTYPE HTML>

<html lang="en" class="loading" ng-app="angulatApp">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
	<title>CHANA RATCHADAWAN Co.,Ltd.</title>
		<meta name="description" content="Dental Laboratory" />
		<meta name="keywords" content="chana,chana ratchadawan,chanaratchadawan,ชนะ รัชดาวรรณ,ฟันปลอม,ฟันเทียม,เรียนทำฟันปลอม,อาชีพทำฟันปลอม,ฟันปลอมทำอย่างไร,อยากทำฟันปลอม,โรงเรียนสอนทำฟันปลอม,สอนทำรีเทนเนอร์,เรียนทำฟันปลอมในกรุงเทพ,เรียนทําฟันที่ไหนดี,แลปทำฟันปลอม,แลปทำฟันปลอม กทม,Clear Retainer,Mouth guards,Nigth guards,ทำฟันปลอมหลักสูตรเต็ม,งานฟันปลอมติดแน่น,Partial Denture,Ceramic,Retainer,Mouth Guards,Veneers,E-Max,หลักสูตร Full Denture,Crown,Inlay,Bridge,ฟันปลอมงานถอดได้,พื้นฐานที่เกี่ยวกับฟันปลอม" />
        
        <meta http-equiv="cache-control" content="max-age=0" />
		<meta http-equiv="cache-control" content="no-cache" />
		<meta http-equiv="expires" content="0" />
		<meta http-equiv="expires" content="Tue, 01 Jan 1980 1:00:00 GMT" />
		<meta http-equiv="pragma" content="no-cache" />
        
		<meta property="og:title" content=""/>
		<meta property="og:image" content=""/>
		<meta property="og:url" content=""/>
		<meta property="og:site_name" content=""/>
		<meta property="og:description" content=""/>
		<meta name="twitter:title" content="" />
		<meta name="twitter:image" content="" />
		<meta name="twitter:url" content="" />
		<meta name="twitter:card" content="" />
		<link rel="stylesheet" href="assets/css/main.css" />   
            
        <meta property="lang_code" content="TH">
	</head>
    
 

    <link rel="stylesheet" type="text/css" href="https://www.scgexpress.co.th/theme/css/bootstrap3.3.7.css"/>
    <link rel="stylesheet" type="text/css" href="https://www.scgexpress.co.th/theme/css/style.css">
    <link rel="stylesheet" type="text/css" href="https://www.scgexpress.co.th/theme/css/styles.css">
    <link rel="stylesheet" type="text/css" href="https://www.scgexpress.co.th/theme/css/font-base.css">
    <link rel="stylesheet" type="text/css" href="https://www.scgexpress.co.th/theme/css/font-awesome.css">
    <link rel="stylesheet" type="text/css" href="https://www.scgexpress.co.th/theme/css/responsive.css">
    <link rel="stylesheet" type="text/css" href="https://www.scgexpress.co.th/theme/css/jquery.mmenu.css"/>
    <link rel="stylesheet" type="text/css" href="https://www.scgexpress.co.th/theme/css/faq.css"/>
    <link rel="stylesheet" type="text/css" href="https://www.scgexpress.co.th/theme/css/selectbox.css"/>
    <link rel="stylesheet" type="text/css" href="https://www.scgexpress.co.th/theme/css/bootstrap-select.css"/>
        <link rel="stylesheet" type="text/css" href="https://www.scgexpress.co.th/theme/css/tooltip.css"/>
    <link rel="stylesheet" type="text/css" href="https://www.scgexpress.co.th/theme/css/radio-checkbox.css"/>
    <link rel="stylesheet" type="text/css" href="https://www.scgexpress.co.th/theme/css/timeline.css"/>

    <link href="https://www.scgexpress.co.th/bower_components/sweetalert2/dist/sweetalert2.min.css" rel="stylesheet">     

    <!-- Link Swiper's CSS -->
    <link rel="stylesheet" type="text/css" href="https://www.scgexpress.co.th/theme/css/swiper.css">

    <!-- JS -->
    <script src="https://www.scgexpress.co.th/theme/js/jquery-3.1.0.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/js/bootstrap.min.js"></script>
    <script src="https://www.scgexpress.co.th/theme/js/bootstrap-select.js"></script>
    <script src="https://www.scgexpress.co.th/theme/js/swiper.min.js"></script>
    <script src="https://www.scgexpress.co.th/theme/js/bar.js"></script>
        <script src="https://www.scgexpress.co.th/theme/js/jquery-qrcode-0.14.0.js" type="text/javascript" charset="utf-8"></script>
    <script src="https://www.scgexpress.co.th/theme/js/TweenMax.min.js" type="text/javascript"></script>
    <script src="https://www.scgexpress.co.th/theme/js/jquery-scrolltofixed-min.js"></script>


    <!-- Modernizr -->
    <script src="https://www.scgexpress.co.th/theme/js/modernizr.js"></script>
    <!-- /// Mobile menu -->
    <script type="text/javascript" src="https://www.scgexpress.co.th/theme/js/jquery.mmenu.all.min.js"></script>
    <script type="text/javascript">
        $(function () {
            $('nav#menu').mmenu();
        });
    </script>
    <!-- ///// end mobile menu -->
    <script>
        window.fbAsyncInit = function () {
            FB.init({
                appId: '164651810668390',
                xfbml: true,
                version: 'v2.8'
            });
        };

        (function (d, s, id) {
            var js, fjs = d.getElementsByTagName(s)[0];
            if (d.getElementById(id)) {
                return;
            }
            js = d.createElement(s);
            js.id = id;
            js.src = "//connect.facebook.net/en_US/sdk.js";
            fjs.parentNode.insertBefore(js, fjs);
        }(document, 'script', 'facebook-jssdk'));


        // function alert_msg(text){
        //     var overlay = jQuery("<div class='overlay_background'></div><div class='alert_msgbox'>" + text + "</div>");
        //     overlay.appendTo(document.body);

        //     var width = Math.floor($('.alert_msgbox').width() / 2);
        //     var height = Math.floor($('.alert_msgbox').height() / 2);

        //     $('.alert_msgbox').css('margin-left','-' + width + 'px');
        //     $('.alert_msgbox').css('margin-top','-' + height + 'px');

        //     setTimeout( "$('.alert_msgbox').fadeOut(300, function() { $('.overlay_background').remove(); });" , 1000 );
        // }


        // $(document).ready(function(){
        //     var msg = '';
        //     alert_msg(msg);
        // });

    </script>

    <!-- black -->
    <!--style>
        html {
            -webkit-filter: grayscale(100%);
            -moz-filter: grayscale(100%);
            -ms-filter: grayscale(100%);
            -o-filter: grayscale(100%);
            filter: grayscale(100%);
            filter: gray;
        }
        img {
            -webkit-filter: grayscale(100%);
            -moz-filter: grayscale(100%);
            -ms-filter: grayscale(100%);
            -o-filter: grayscale(100%);
            filter: grayscale(100%);
            filter: gray;
        }
    </style-->

</head>
<!-- END HEAD -->
<!-- ============= Loader ========== -->
<style>

    html, body {
        min-height: 100%;
    }

    html.loading {
        /*background: #ffffff url('

    https://www.scgexpress.co.th/  theme/images/loader.gif') no-repeat 50% 50%;*/
        -webkit-transition: background-color 0;
        transition: background-color 0;
    }

    body {
        -webkit-transition: opacity 0.8s ease-in;
        transition: opacity 0.8s ease-in;
    }

    html.loading body {
        opacity: 0;
        -webkit-transition: opacity 0;
        transition: opacity 0;
    }

    .mm-page {
        height: 100%;
    }

    #box-selbox {
        color: #989898;
        background-color: #f4f4f4;
        border-top: 5px solid #ed1b24;
        display: none;
        text-align: justify;
        float: left;
        margin: 20px 0;
        width: 420px;
        /*height: 500px;*/
    }

    .size-radio {
        position: relative;
        width: 78%;
        margin: 0 auto;
    }

    .size-radio ul {
        list-style: none;
        margin: 0;
        padding: 0;
        overflow: auto;
    }

    .size-radio ul li {
        color: #989898;
        display: block;
        position: relative;
        float: left;
        width: 50%;
    }

    .size-radio ul li input[type=radio] {
        position: absolute;
        visibility: hidden;
    }

    .dropdown-menu {
        margin: 0 0 1.5em 0em !important;
        min-width: 280px !important;
    }

    .dropdown-menu > li > a:focus {
        color: #fff;
        text-decoration: none;
        background-color: #ed1c24;
    }
    .dropdown-menu > .active > a,
    .dropdown-menu > .active > a:hover,
    .dropdown-menu > .active > a:focus {
        color: #fff;
        text-decoration: none;
        background-color: #ed1b25;
        outline: 0;
    }

    ul li label {
        display: block;
        position: relative;
        padding: 0 25px 10px 35px;
        margin: 0 auto;
        z-index: 9;
        cursor: pointer;
        -webkit-transition: all 0.25s linear;
        font-size: 16px;
        line-height: 16px;
    }

    .size-radio ul li:hover label {
        color: #989898;
    }

    .size-radio ul li .check {
        display: block;
        position: absolute;
        border: 3px solid #ce060f;
        border-radius: 100%;
        height: 25px;
        width: 25px;
        top: 0;
        left: 0;
        z-index: 5;
        transition: border .25s linear;
        -webkit-transition: border .25s linear;
    }

    .size-radio ul li:hover .check {
        border: 3px solid #ce060f;
    }

    .size-radio ul li .check::before {
        display: block;
        position: absolute;
        content: '';
        border-radius: 100%;
        height: 8px;
        width: 8px;
        top: 5px;
        left: 5px;
        margin: auto;
        transition: background 0.25s linear;
        -webkit-transition: background 0.25s linear;
    }

    input[type=radio]:checked ~ .check {
        border: 3px solid #ce060f;
    }

    input[type=radio]:checked ~ .check::before {
        background: #ed1b24;
    }

    input[type=radio]:checked ~ label {
        color: #989898;
    }

    .switch {
        padding: 20px 0;
    }

    .cmn-toggle {
        position: absolute;
        margin-left: -9999px;
        visibility: hidden;
    }

    .cmn-toggle + label {
        display: block;
        position: relative;
        cursor: pointer;
        outline: none;
        -webkit-user-select: none;
        -moz-user-select: none;
        -ms-user-select: none;
        user-select: none;
    }

    input.cmn-toggle-round + label {
        padding: 0;
        width: 229px;
        height: 48px;
        margin: 0 auto;
        background: url("https://www.scgexpress.co.th/theme/images/bg-bt-switch.png") no-repeat;
    }

    input.cmn-toggle-round + label:before, input.cmn-toggle-round + label:after {
        display: block;
        position: absolute;
        top: 0;
        left: 0;
        bottom: 0;
        content: "";
    }

    input.cmn-toggle-round + label:before {
        right: 0;
        -webkit-transition: background 0.4s;
        -moz-transition: background 0.4s;
        -o-transition: background 0.4s;
        transition: background 0.4s;
    }

    input.cmn-toggle-round + label:after {
        width: 128px;
        background: url("https://www.scgexpress.co.th/theme/images/bt-switch-dry.png") no-repeat;
        -webkit-transition: margin 0.4s;
        -moz-transition: margin 0.4s;
        -o-transition: margin 0.4s;
        transition: margin 0.4s;
    }

    input.cmn-toggle-round:checked + label:before {
        /*background-color: #8ce196;*/
    }

    input.cmn-toggle-round:checked + label:after {
        margin-left: 101px;
        background: url("https://www.scgexpress.co.th/theme/images/bt-switch-cool.png") no-repeat;
    }

    select {

        -webkit-border-radius: 10px;
        -moz-border-radius: 10px;
        border-radius: 10px;

        font-family: "db_helvethaica_x55_regular";
        font-size: 16px;
        line-height: 16px;
        padding: 5px 20px !important;
        text-align: center;

        /*background: #ffffff none repeat scroll 0 0 !important;*/
        width: 100%;
        border: 0 solid #ccc;

        -webkit-appearance: none;
        -moz-appearance: none;
        appearance: none;

        background: url("https://www.scgexpress.co.th/theme/images/down_arrow.png") 90% / 20% no-repeat #f4f4f4;

        height: 32px;
    }

    input[disabled] {
        cursor: default;
        background: #ececec !important;
    }


</style>
<script>
    var html = document.getElementsByTagName('html')[0];
    var removeLoading = function () {
        // In a production application you would remove the loading class when your
        // application is initialized and ready to go.  Here we just artificially wait
        // 3 seconds before removing the class.
        setTimeout(function () {
            html.className = html.className.replace(/loading/, '');
        }, 800);
    };
    removeLoading();
</script>

<!-- =================== -->
<!-- BEGIN BODY -->
<body>
<script>
    (function (i, s, o, g, r, a, m) {
        i['GoogleAnalyticsObject'] = r;
        i[r] = i[r] || function () {
            (i[r].q = i[r].q || []).push(arguments)
        }, i[r].l = 1 * new Date();
        a = s.createElement(o),
            m = s.getElementsByTagName(o)[0];
        a.async = 1;
        a.src = g;
        m.parentNode.insertBefore(a, m)
    })(window, document, 'script', 'https://www.google-analytics.com/analytics.js', 'ga');

    ga('create', 'UA-74056591-4', 'auto');
    ga('send', 'pageview');

</script>


<!-- Black Ribbon  -->

<div id="page">

    <div class="head-bar">
        <div class="head-bar-fixed">

            
                
            </header>

          <style type="text/css">
    .black-ribbon {
        position: fixed;
        z-index: 9999;
        width: 70px;
    }

    @media only all and (min-width: 768px) {
        .black-ribbon {
            width: auto;
        }
    }

    .stick-left {
        left: 0;
    }

    .stick-right {
        right: 0;
    }

    .stick-top {
        top: 0;
    }

    .stick-bottom {
        bottom: 0;
    }
            </style>
        </div>
    </div><!-- >>>>>>>>>>>>>>>>>>>>>> Pop up preload <<<<<<<<<<<<<<<<<<<<<<<<<< -->
<style type="text/css">
    .preload-div {
        width: 100%;
        height: 100vh;
        z-index: 9999;
        position: absolute;
        background-color: rgba(255, 255, 255, 0.9);

    }

    .spiner-load {
        position: absolute;
        top: 33%;
        left: 50%;
        margin-top: -25px;
        margin-left: -25px;
    }

    .preload-hide {
        display: none;
    }

    .preload-show {
        display: block;
    }
</style>

<div class="preload-div preload-hide">
    <div class="spiner-load">
        <img src="/images/loader.gif" style="width:50px;">
    </div>
</div>


<!-- >>>>>>>>>>>>>>>>>>>>>>>>> End pop up <<<<<<<<<<<<<<<<<<<<<<<<<<<<< -->

<div class="bg-shipping">
    <div class="ed-container-home">
        <div class="head-txt-base clear">
           
            <div class="head-txt">
                ติดต่อเรา
          </div>
            
            <div class="pickup-txt">
				หากสนใจต้องการเรียนกับเรา สามารถแจ้งรายละเอียดตามด้านล่าง<br>เมื่อกรอกรายละเอียดครบถ้วน ทางเจ้าหน้าที่จะทำการติดต่อกลับตามเบอร์โทรที่ท่านแจ้งไว้
               
          </div>



            <!-- System -->
            
            <div class="box-pickup">
            	<div class="pickup-row"><input type="text" class="size-contact-txtbox name" placeholder="ชื่อ-สกุล Name"></div>
				<div class="pickup-row"><input type="text" class="size-contact-txtbox tel" id="txtboxToFilter" maxlength="10" placeholder="เบอร์โทร Tel."></div>
           		<div class="pickup-row">
           			 <textarea id="pickup_address" placeholder="ที่อยู่ Address" class="call-area-address pickup-textarea from_address"></textarea>
           		</div>
           		<div class="pickup-row">
           			<textarea id="remark_note" name="remark_note" placeholder="รายละเอียดอื่นๆ" class="call-area-address pickup-textarea2 remark_note"></textarea>
           		</div>
            </div>
            
         
          <div class="size-bt-pickup">
            	 <button id="accept">ตกลง</button>
            </div>
            
            

          <div class="size-pop-bt">
            </div>

        </div>
    </div>
</div>

<script>

    var markers = [];

    function initMap() {

        var map = new google.maps.Map(document.getElementById('map'), {
            zoom: 18,
            center: {lat: 13.7248936, lng: 100.4930265}
        });

        var geocoder = new google.maps.Geocoder();

        // Try HTML5 geolocation.
        if (navigator.geolocation) {
            navigator.geolocation.getCurrentPosition(function (position) {
                var pos = {
                    lat: position.coords.latitude,
                    lng: position.coords.longitude
                };

                // alert("lat : " + position.coords.latitude + ", lng : " + position.coords.longitude );

                $("#address").val(position.coords.latitude + ", " + position.coords.longitude);
                $("#submit").click();
                return;

                // var geocoder = new google.maps.Geocoder;
                // var infowindow = new google.maps.InfoWindow;

                // var lat = position.coords.latitude;
                // var lng = position.coords.longitude;

                // getAddress(lat, lng, geocoder, map, infowindow);

            }, function () {
                handleLocationError(true, infoWindow, map.getCenter());
            });

        } else {

            // Browser doesn't support Geolocation
            handleLocationError(false, infoWindow, map.getCenter());

        }

        document.getElementById('submit').addEventListener('click', function () {
            geocodeAddress(geocoder, map);
        });


        // dragend


    }


    function eventFire(el, etype) {
        if (el.fireEvent) {
            el.fireEvent('on' + etype);
        } else {
            var evObj = document.createEvent('Events');
            evObj.initEvent(etype, true, false);
            el.dispatchEvent(evObj);
        }
    }

    function geocodeAddress(geocoder, resultsMap) {
        var address = document.getElementById('address').value;
        geocoder.geocode({'address': address}, function (results, status) {
            if (status === 'OK') {

                var needle = "postal_code";
                var result_postcode = "";

                // iterate over each element in the array
                for (var i = 0; i < results[0].address_components.length; i++) {
                    // look for the entry with a matching `code` value
                    if (results[0].address_components[i].types[0] == needle) {
                        // we found it
                        result_postcode = results[0].address_components[i].long_name;
                        // obj[i].name is the matched result
                    }
                }

                console.log(results[0]);

                // alert("POSTCODE : " + result_postcode);

                var contentString = '<div class="map_address">' + results[0].address_components[1].long_name + "<hr>" + results[0].formatted_address + '</div>';

                var infowindow = new google.maps.InfoWindow({
                    content: contentString,
                    maxWidth: 250
                    // content: results[0].formatted_address;
                });

                resultsMap.setCenter(results[0].geometry.location);

                if (markers.length > 0) {

                    // Clear map
                    markers[0].setMap(null);
                    markers = [];

                    var marker = new google.maps.Marker({
                        map: resultsMap,
                        position: results[0].geometry.location,
                        draggable: true,
                        title: result_postcode,
                        zoom: 16,
                    });

                } else {

                    var marker = new google.maps.Marker({
                        map: resultsMap,
                        position: results[0].geometry.location,
                        draggable: true,
                        title: result_postcode,
                        zoom: 16
                    });

                }

                markers.push(marker);
                infowindow.open(map, marker);

                // marker.addListener('click', function(){
                //   alert("OK");
                // });

                google.maps.event.addListener(marker, 'dragend', function (event) {

                    // var geocoder = new google.maps.Geocoder;
                    // var infowindow = new google.maps.InfoWindow;
                    var lat = this.getPosition().lat();
                    var lng = this.getPosition().lng();

                    // alert(lat + ", " + lng);

                    $("#address").val(lat + ", " + lng);
                    $("#submit").click();

                });


            } else {
                alert('Geocode was not successful for the following reason: ' + status);
            }
        });

    }


    function getAddress(lat, lng, geocoder, map, infowindow) {

        // alert("IN");
        // alert(lat + ", " + lng);

        var latlng = {lat: lat, lng: lng};
        geocoder.geocode({'location': latlng}, function (results, status) {

            console.info(results);

            if (status === 'OK') {
                if (results[0]) {
                    map.setZoom(11);
                    var marker = new google.maps.Marker({
                        position: latlng,
                        map: map,
                        draggable: true
                    });


                    google.maps.event.addListener(marker, 'dragend', function (event) {

                        // var geocoder = new google.maps.Geocoder;
                        // var infowindow = new google.maps.InfoWindow;
                        var lat = this.getPosition().lat();
                        var lng = this.getPosition().lng();

                        // alert(lat + ", " + lng);

                        $("#address").val(lat + ", " + lng);
                        $("#submit").click();

                    });

                    // infowindow.setContent(results[0].formatted_address);
                    // infowindow.setContent(results[0].address_components[0]);

                    // the code you're looking for
                    var needle = "postal_code";

                    // iterate over each element in the array
                    for (var i = 0; i < results[0].address_components.length; i++) {
                        // look for the entry with a matching `code` value
                        if (results[0].address_components[i].types[0] == needle) {
                            // we found it
                            infowindow.setContent(results[0].address_components[i].long_name);
                            // obj[i].name is the matched result
                        }
                    }

                    infowindow.open(map, marker);

                } else {
                    window.alert('No results found');
                }
            } else {
                window.alert('Geocoder failed due to: ' + status);
            }

        });

    }

    function handleLocationError(browserHasGeolocation, infoWindow, pos) {
        infoWindow.setPosition(pos);
        infoWindow.setContent(browserHasGeolocation ? 'Error: The Geolocation service failed.' : 'Error: Your browser doesn\'t support geolocation.');
        infoWindow.open(map);
    }

    

</script>
<script async defer
        src="https://maps.googleapis.com/maps/api/js?key=AIzaSyAusYm27BYqR2_NAQdCJLYJnbJiLtxY_aY&callback=initMap">
</script>

<script type="text/javascript">

    $(document).on("click", "#call_popup", function () {

        $("#showpopup").show();

    });


    function checktel(tel) {
        var reg = "/^\d+$/";
        var patt = new RegExp(reg);
        //patt.test(phone_number);

        if (checktel(tel) === true) {
            //alert("เบอร์ถูกต้องแล้ว");
        } else {
            alert("เบอร์โทรศัพท์ไม่ถูกต้อง");
            $(".tel").val("");
        }
    }


    $(document).on("keyup", "#txtboxToFilter", function (e) {

        var pattern = /^\d+$/;

        if (pattern.test($(this).val()) !== true) {
            alert("กรุณาใช้ตัวเลขเท่านั้น");
            $(this).val("");
            return;
        }

        var num = $(this).val();
        var num_check_is_phone_number = num.substring(0, 1);
        var num_check = num.substring(0, 2);

        if (num_check_is_phone_number != 0) {
            alert("รูปแบบเบอร์โทรศัพท์ไม่ถูกต้อง");
            $(this).val("");
            e.preventDefault();
            // return;
        }

        // alert(num_check);

        if (num_check == "02" || num_check == "03" || num_check == "04" || num_check == "05" || num_check == "07") {
            console.log("IF");
            $(this).attr("maxlength", "9");
        } else {
            console.log("ELSE");
            $(this).attr("maxlength", "10");
        }

    });


    $(document).on("keydown", "#txtboxToFilter", function (e) {

        // Allow: backspace, delete, tab, escape, enter and .
        if ($.inArray(e.keyCode, [46, 8, 9, 27, 13, 110, 190]) !== -1 ||
            // Allow: Ctrl+A, Command+A
            (e.keyCode === 65 && (e.ctrlKey === true || e.metaKey === true)) ||
            // Allow: home, end, left, right, down, up
            (e.keyCode >= 35 && e.keyCode <= 40)) {
            // let it happen, don't do anything
            return;
        }
        // Ensure that it is a number and stop the keypress
        if ((e.shiftKey || (e.keyCode < 48 || e.keyCode > 57)) && (e.keyCode < 96 || e.keyCode > 105)) {
            e.preventDefault();
        } else {
            console.log($(this).val());
        }
    });


    $(document).on("click", "#accept", function () {

        var today = new Date();
        var h = today.getHours().toString();
        var m = today.getMinutes().toString();
        var d = today.getDay().toString();
        msg = '';

        console.log(today);
        console.log(h);
        console.log(m);
        console.log(d);

        // 08:30 - 17:30

        if (d != 7) {

            if (h > 7 && h < 15) {

                // alert("เข้าข่าย");

                if (h == 8 && m < 30) {
                    alert("ยังไม่เปิดบริการ เราจะเปิดบริการเวลา 08:30");
                    return;
                }

                if (h == 17 && m > 30) {

                    if (confirm("ขณะนี้เป็นเวลานอกทำการ เจ้าหน้าที่จะติดต่อกลับในเวลาทำการถัดไป") == true) {
                        msg = 'out_of_office_hour';
                    } else {
                        location.reload();
                        return;
                    }

                    // alert("ปิดบริการแล้ว");
                    // return;
                }


            } else {

                // alert("ขณะนี้เป็นเวลานอกทำการ เจ้าหน้าที่จะติดต่อกลับในวันทำการถัดไป");

                if (confirm("ขณะนี้เป็นเวลานอกทำการ เจ้าหน้าที่จะติดต่อกลับในเวลาทำการถัดไป") == true) {
                    msg = 'out_of_office_hour';
                } else {
                    location.reload();
                    return;
                }

            }

        } else {

            if (confirm("ขณะนี้เป็นเวลานอกทำการ เจ้าหน้าที่จะติดต่อกลับในเวลาทำการถัดไป") == true) {
                msg = 'out_of_office_hour';
            } else {
                location.reload();
                return;
            }

        }

        // alert('บันทึกข้อมูล');
        // return;

        obj = $(this).parents(".head-txt-base:first");
        name = obj.find(".name").val();
        tel = obj.find(".tel").val();
        address = obj.find(".from_address").val();
        remark_note = obj.find(".remark_note").val();

        //checktel(tel);

        // var reg = /^\d+$/;
        // var patt = new RegExp(reg);
        // //patt.test(phone_number);

        // if( checktel(tel) === true ){
        // 	//alert("เบอร์ถูกต้องแล้ว");
        // }else{
        // 	alert("เบอร์ไม่ถูกต้อง");
        // 	$(".tel").val("");
        // 	return;
        // }

        site = 'https://' + window.location.host + '/' + "pickup/";

        if (name != "" && tel != "" && address != "") {

            // $(".preload-hide");

            $.ajax({
                url: site + 'request_pickup',
                type: "POST",
                cache: false,
                dataType: 'json',
                data: {
                    contact_name: name,
                    contact_tel: tel,
                    from_address: address,
                    remark_note: remark_note

                }
            }).done(function (data) {

                console.log(data);

                if (data.status == 'error') {
                    alert("มีบางอย่างผิดพลาด");
                } else {

                    if (msg != 'out_of_office_hour') {
                        alert("การส่งข้อความสำเร็จ กรุณารอการติดต่อกลับจากเจ้าหน้าที่");
                    }

                    $("#close").click();

                    location.reload(true);
                }

            });

        } else {

            alert("กรุรณาใส่ข้อมูลให้ถูกต้อง");
            obj.find(".name").val("");
            obj.find(".tel").val("");
            obj.find(".address").val("");
            obj.find(".remark_note").val("");

        }

    });


    $(document).on("click", "#close", function () {
        $(this).parents("#showpopup").fadeOut(1000, function () {
        });
    });



//    alert("เนื่องจากเป็นวันหยุดประจำปีบริษัท กรุณาแจ้งให้เข้ารับพัสดุถึงที่ (Pick up service) ก่อนเวลา 10:00 น.");
</script>


<style type="text/css">


    #call_popup {
        margin: 150px;
    }

    .txt_pop input {
        border: 1px solid #e8e7e7;
        margin: 5px;
        width: 100%;
    }

    .map_address {
        /*width: 250px;	*/
    }

</style>

</div>

<script src="https://www.scgexpress.co.th/theme/js/faq.js"></script>

<script src="https://www.scgexpress.co.th/theme/js/timeline-tweenmax-banner.js" type="text/javascript"></script>
<!-- Start of StatCounter Code for Default Guide -->
<script type="text/javascript">
var sc_project=11244805; 
var sc_invisible=1; 
var sc_security="70cddaf6"; 
</script>
<script type="text/javascript" async src="//www.statcounter.com/counter/counter.js"></script>
<noscript><div class="statcounter"><a title="website
statistics" href="https://statcounter.com/"
target="_blank"></a></div></noscript>
<!-- End of StatCounter Code for Default Guide -->
</body>
<!-- END BODY -->
</html>  
